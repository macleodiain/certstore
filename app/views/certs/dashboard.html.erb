<div class="cert-display-card">
  <% @certs.each do |cert| %>
    <div class="cert">
      <div class="details-panel">

        <p class="name"><%= link_to cert.name.upcase, cert_path(cert), class: "cert-link" %></p><br>
        <% if cert.expires.nil? %>
          <p class="expires">Does not expire</p>
        <% else %>
          <p class="expires">Expiry Date: <%= cert.expires.strftime("%d / %m / %y") %> </p>
          <p class="expires"><%= cert.expires_in %></p>
        <% end %>
      </div>
      <div class="image-panel">
        <p><%= image_tag cert.image, class: "cert-image" %></p>
      </div>

    </div>
  <% end %></div>